# conda env create --file environment.yml
name: spotify_music_recommender
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.10
  - pip:
    - pyhere
    